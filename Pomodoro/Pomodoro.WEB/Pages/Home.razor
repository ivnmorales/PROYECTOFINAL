@page "/"
@inject NavigationManager NavigationManager

<div class="pomodoro-home-container">
    <h1 class="animate__animated animate__zoomIn">¡Bienvenido a FocusFlow!</h1>
    <h2 class="animate__animated animate__fadeInUp">La manera más divertida de mejorar la productividad en el aula.</h2>

    <div class="main-features">
        <div class="feature-card animate__animated animate__fadeInUp">
            <i class="fas fa-tasks fa-3x"></i>
            <h3>Gestión de Tareas</h3>
            <p>Asignar proyectos y controlar el progreso de los estudiantes nunca fue tan fácil.</p>
        </div>
        <div class="feature-card animate__animated animate__fadeInUp">
            <i class="fas fa-clock fa-3x"></i>
            <h3>Sesiones Pomodoro</h3>
            <p>¡Aprovecha el poder del Pomodoro para maximizar la concentración!</p>
        </div>
        <div class="feature-card animate__animated animate__fadeInUp">
            <i class="fas fa-medal fa-3x"></i>
            <h3>Recompensas</h3>
            <p>Premia el esfuerzo de tus estudiantes y motívalos a seguir adelante.</p>
        </div>
    </div>

    <div class="action-buttons">
        <button class="btn btn-primary" @onclick="GoToDashboard">Empecemos</button>
        <a href="/register" class="btn btn-secondary">Regístrate</a>
        <a href="/login" class="btn btn-secondary">Inicia Sesión</a>
    </div>
</div>

@code {
    private void GoToDashboard()
    {
        NavigationManager.NavigateTo("/dashboard");
    }
}
